# 🔧 Решение без поля "Команда запуска"

## Проблема
В интерфейсе Timeweb нет поля "Команда запуска", но оно требуется для React приложения.

## Решение 1: Измени команду сборки (РЕКОМЕНДУЕТСЯ)

В поле **"Команда сборки"** укажи:

```
npm install && npm run build && npm start
```

Это соберет проект И сразу запустит его.

---

## Решение 2: Через переменные окружения

1. Нажми кнопку **"Добавить"** в разделе "Переменные"
2. Добавь переменную:
   - **Имя:** `START_COMMAND`
   - **Значение:** `npm start`

Или попробуй:
   - **Имя:** `NPM_START`
   - **Значение:** `serve -s dist -l 3000`

---

## Решение 3: Создай скрипт запуска

Создай файл `start.sh` в корне проекта:

```bash
#!/bin/bash
npm install
npm run build
npm start
```

И в поле **"Команда сборки"** укажи:
```
bash start.sh
```

---

## Решение 4: Используй статический хостинг (ПРОЩЕ ВСЕГО)

Если App Platform не работает, используй статический хостинг:

### Шаг 1: Собери проект локально
```bash
npm install
npm run build
```

### Шаг 2: Загрузи папку `dist/` на Timeweb Hosting
1. В панели Timeweb перейди в **"Хостинг"**
2. Создай новый сайт или используй существующий
3. Загрузи все файлы из папки `dist/` в корень сайта (через FTP или файловый менеджер)

### Шаг 3: Настрой домен
- Привяжи домен к хостингу
- Или используй временный домен от Timeweb

**Преимущества:**
- ✅ Не нужна команда запуска
- ✅ Проще и дешевле
- ✅ Работает сразу

---

## Решение 5: Измени package.json (если возможно)

Попробуй изменить скрипт `build` в `package.json`:

```json
"scripts": {
  "build": "vite build && npm start",
  "start": "serve -s dist -l 3000"
}
```

Тогда команда сборки `npm run build` автоматически запустит сервер.

---

## Рекомендация

**Попробуй в таком порядке:**

1. **Сначала:** Измени команду сборки на `npm install && npm run build && npm start`
2. **Если не работает:** Используй статический хостинг (Решение 4) - это проще всего
3. **Если нужен App Platform:** Попробуй через переменные окружения

---

## Быстрая проверка

Перед деплоем проверь локально:
```bash
npm install
npm run build
npm start
```

Если работает локально, но не работает на Timeweb - используй статический хостинг.

---

**Самый простой вариант - статический хостинг!** Просто загрузи папку `dist/` на хостинг. 🚀

