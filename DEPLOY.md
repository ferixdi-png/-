# 🚀 Инструкция по деплою на Timeweb

## ✅ Подготовка проекта

### Шаг 1: Проверка зависимостей

Все Google-зависимости уже удалены:
- ✅ Google Fonts → заменены на Bunny Fonts
- ✅ Google CDN → заменены на esm.sh
- ✅ Gemini API → отключен (заглушка)

### Шаг 2: Сборка проекта

```bash
# Установка зависимостей
npm install

# Сборка проекта
npm run build
```

После сборки будет создана папка `dist/` с готовыми файлами.

---

## 📦 Деплой на Timeweb App Platform

### Вариант 1: Через веб-интерфейс Timeweb

1. **Войди в панель Timeweb Cloud**
   - Перейди на https://timeweb.cloud
   - Войди в свой аккаунт

2. **Создай новое приложение**
   - Перейди в раздел **"App Platform"**
   - Нажми **"Создать приложение"** или **"Добавить App"**

3. **Шаг 1: Выбери тип приложения**
   - Выбери вкладку **"Frontend"** (она должна быть активна)
   - В списке фреймворков выбери **"React"** (синяя иконка с атомом)
   - Или выбери **"Другой"** (если React недоступен)
   - ⚠️ **Важно:** Выбирай только SSG/SPA (наш проект — это SPA после сборки)

4. **Шаг 2: Настройки приложения**

   **Источник кода:**
   - Выбери **"GitHub"** (если залил в репозиторий) или **"ZIP"** (если загружаешь архив)
   - Если GitHub: подключи репозиторий и укажи ветку (обычно `main` или `master`)
   - Если ZIP: загрузи архив всего проекта (не только папку `dist/`)

   **Настройки сборки (если есть поля для ввода):**
   ```
   Runtime: Node.js 24 (или последняя доступная версия)
   Build Command: npm install && npm run build
   Start Command: npm start
   Output Directory: dist
   Port: 3000
   ```

   **Переменные окружения:**
   - Не требуются (API отключен, можно оставить пустым)

5. **Шаг 3: Регион и тариф**
   - Выбери регион: **Москва** (или ближайший к тебе)
   - Выбери тариф: **99₽/мес** (базовый, 200 тыс запросов/мес, 1 ГБ NVMe)
   - Этого достаточно для старта

6. **Деплой**
   - Нажми **"Заказать"** или **"Создать"**
   - Дождись завершения сборки (обычно 2-5 минут)
   - Получишь URL типа: `https://your-app-abc123.timeweb.app`

### ⚠️ Важные моменты:

- **Если выбран React:** Timeweb автоматически определит настройки сборки
- **Если выбран "Другой":** Нужно будет вручную указать команды сборки
- **После деплоя:** Проверь, что сайт открывается и все работает

---

### Вариант 2: Через Timeweb Cloud Functions (альтернатива)

Если App Platform не подходит, можно использовать статический хостинг:

1. **Собери проект:**
   ```bash
   npm run build
   ```

2. **Загрузи папку `dist/` на Timeweb Hosting**
   - Используй FTP или файловый менеджер
   - Загрузи все файлы из `dist/` в корень сайта

3. **Настрой домен** (если нужно)

---

## 🔍 Проверка после деплоя

### 1. Открой сайт в браузере
- Перейди по полученному URL
- Проверь, что сайт загружается

### 2. Проверь в DevTools (F12 → Network)
- **НЕ должно быть** запросов к:
  - `*.googleapis.com`
  - `*.google.com`
  - `*.gstatic.com`
  - `generativelanguage.googleapis.com`

- **Должны быть** запросы к:
  - `fonts.bunny.net` (шрифты)
  - `esm.sh` (React модули)
  - `cdn.tailwindcss.com` (Tailwind CSS)

### 3. Проверь в РФ без VPN
- Открой сайт с российского IP
- Все должно загружаться без проблем

---

## 🐛 Решение проблем

### Проблема: Сайт не собирается
**Решение:**
```bash
# Очисти кэш и переустанови зависимости
rm -rf node_modules package-lock.json
npm install
npm run build
```

### Проблема: Ошибка "Cannot find module"
**Решение:**
- Убедись, что все зависимости в `package.json`
- Проверь, что `npm install` выполнен успешно

### Проблема: Сайт не открывается
**Решение:**
- Проверь, что порт указан правильно (3000)
- Проверь логи в панели Timeweb
- Убедись, что команда `npm start` работает локально

### Проблема: Есть запросы к Google
**Решение:**
- Проверь все файлы через поиск:
  ```bash
  grep -r "googleapis\|gstatic\|google" dist/
  ```
- Если нашел — замени в исходниках и пересобери

---

## 📝 Структура проекта для деплоя

```
ai-bot-business-plan/
├── dist/                    # ← Это загружается на хостинг
│   ├── index.html
│   ├── assets/
│   └── ...
├── package.json
├── vite.config.ts
└── ...
```

**Важно:** На Timeweb загружается только папка `dist/` после сборки.

---

## 🎯 Быстрая команда для проверки

```bash
# Собери проект
npm run build

# Проверь, что нет Google-зависимостей
grep -r "googleapis\|gstatic" dist/ || echo "✅ Все чисто!"

# Запусти локально для проверки
npm start
```

---

## 📞 Поддержка

Если возникли проблемы:
- Напиши в Telegram: @dmitriy_ferixdi
- Или создай issue в репозитории

---

## ✅ Чеклист перед деплоем

- [ ] `npm install` выполнен успешно
- [ ] `npm run build` выполнен без ошибок
- [ ] Проверено, что нет Google-зависимостей
- [ ] Сайт работает локально (`npm start`)
- [ ] Все секции отображаются корректно
- [ ] Мобильная версия работает
- [ ] Все ссылки на Telegram работают

---

**Готово!** 🎉 Твой сайт готов к работе в РФ без VPN.

