# ✅ Финальная настройка для Timeweb

## Что изменено в коде

### 1. Создан файл `server.js`
- Простой Node.js сервер для запуска статического сайта
- Работает с папкой `dist/`
- Поддерживает все типы файлов (HTML, CSS, JS, изображения и т.д.)

### 2. Изменен `package.json`
- Команда `start` теперь: `node server.js`
- Удалена зависимость `serve` (не нужна, используем свой сервер)

---

## Настройки в Timeweb

### В разделе "5. Настройка приложения" укажи:

**1. Команда сборки:**
```
npm install && npm run build
```

**2. Зависимости:**
Оставь пустым или укажи:
```
npm install
```

**3. Директория сборки:**
```
dist
```
⚠️ **Важно:** Не `/build`, а именно `dist`

**4. Команда запуска (если есть отдельное поле):**
```
npm start
```
Или:
```
node server.js
```

**5. Переменные:**
Не добавляй ничего

---

## Как это работает

1. **Сборка:** `npm install && npm run build`
   - Устанавливает зависимости
   - Собирает проект в папку `dist/`

2. **Запуск:** `npm start` → `node server.js`
   - Запускает простой HTTP сервер
   - Раздает файлы из папки `dist/`
   - Работает на порту 3000 (или из переменной PORT)

---

## Проверка локально

Перед деплоем проверь локально:

```bash
# 1. Установи зависимости
npm install

# 2. Собери проект
npm run build

# 3. Запусти сервер
npm start
```

Открой http://localhost:3000 - должно работать!

---

## Если в Timeweb нет поля "Команда запуска"

### Вариант 1: Добавь в переменные окружения
1. Нажми "Добавить" в разделе "Переменные"
2. Добавь:
   - **Имя:** `START_COMMAND`
   - **Значение:** `npm start`

### Вариант 2: Измени команду сборки
Если Timeweb не поддерживает отдельную команду запуска, попробуй:
```
npm install && npm run build && npm start
```

Но лучше использовать отдельные команды, если есть такая возможность.

---

## Итоговые настройки

```
Команда сборки: npm install && npm run build
Зависимости: npm install (или пусто)
Директория сборки: dist
Команда запуска: npm start (или node server.js)
Порт: 3000 (или оставь пустым)
```

---

## После деплоя

1. Дождись завершения сборки
2. Проверь логи - должны быть сообщения:
   - ✅ "Build successful"
   - ✅ "Server running on http://0.0.0.0:3000"
3. Открой URL приложения
4. Проверь в DevTools (F12 → Network):
   - ✅ Нет запросов к Google
   - ✅ Сайт загружается

---

**Готово! Теперь команды сборки и запуска разделены, как требует Timeweb.** 🚀

