# 🔧 Исправление белого экрана

## Проблема
Сайт задеплоился, но показывает белый экран.

## Причина
Сервер не правильно обрабатывает маршруты React SPA. Нужно, чтобы все запросы (кроме статических файлов) возвращали `index.html`.

## Решение

Я исправил `server.js` - теперь он:
1. Отдает статические файлы (CSS, JS, изображения) если они существуют
2. Для всех остальных путей отдает `index.html` (для React Router)

---

## Что делать сейчас

### Вариант 1: Пересобери и задеплой заново

1. **Локально проверь:**
```bash
npm install
npm run build
npm start
```

2. **Открой http://localhost:3000** - должно работать

3. **Если работает локально:**
   - Закоммить изменения в Git
   - Задеплой заново на Timeweb

### Вариант 2: Проверь логи деплоя

В панели Timeweb:
1. Открой свое приложение
2. Перейди на вкладку "История" или "Логи"
3. Проверь, нет ли ошибок при сборке

---

## Проверка в браузере

### Открой DevTools (F12)

1. **Вкладка Console:**
   - Проверь, нет ли ошибок JavaScript
   - Ошибки типа "Cannot find module" или "Failed to load" указывают на проблему

2. **Вкладка Network:**
   - Обнови страницу (F5)
   - Проверь, загружается ли `index.html` (должен быть статус 200)
   - Проверь, загружаются ли файлы из папки `assets/`
   - Если есть ошибки 404 - файлы не собрались правильно

---

## Возможные проблемы

### Проблема 1: Файлы не собрались
**Решение:**
- Проверь логи деплоя в Timeweb
- Убедись, что команда сборки: `npm install && npm run build`
- Убедись, что директория сборки: `dist`

### Проблема 2: Неправильные пути к файлам
**Решение:**
- В `vite.config.ts` проверь настройку `base`
- Для деплоя на поддомен может понадобиться указать `base: '/'`

### Проблема 3: Ошибки JavaScript в консоли
**Решение:**
- Открой DevTools → Console
- Скопируй ошибки
- Проверь, что все зависимости установлены

---

## Быстрая проверка

### 1. Проверь, что файлы собрались:
```bash
npm run build
ls dist/  # или dir dist на Windows
```

Должны быть:
- `index.html`
- `assets/` (папка с JS и CSS)

### 2. Проверь локально:
```bash
npm start
```

Открой http://localhost:3000 - должно работать.

### 3. Если локально работает, но на Timeweb нет:
- Проблема в настройках деплоя
- Проверь логи в Timeweb
- Убедись, что директория сборки: `dist`

---

## Альтернативное решение

Если проблема не решается, можно использовать статический хостинг:

1. Собери проект: `npm run build`
2. Загрузи папку `dist/` на Timeweb Hosting
3. Настрой `.htaccess` для SPA (если нужно)

Но сначала попробуй пересобрать с исправленным `server.js`.

---

**Главное:** Пересобери проект с исправленным `server.js` и задеплой заново! 🚀

