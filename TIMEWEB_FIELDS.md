# 📝 Что указать в полях Timeweb

## Раздел "5. Настройка приложения"

### 1. Команда сборки
```
npm install && npm run build
```

**Или просто:**
```
npm run build
```
(Если зависимости устанавливаются автоматически)

---

### 2. Зависимости
Оставь **ПУСТЫМ** или укажи:
```
npm install
```

**Почему:** Timeweb обычно сам устанавливает зависимости из `package.json`, но можно указать явно для надежности.

---

### 3. Директория сборки ⚠️ ВАЖНО!
```
dist
```

**НЕ `/build`** — у нас Vite, он собирает в папку `dist`, а не `build`!

Если там написано `/build` — **замени на `dist`**

---

### 4. Переменные
**НЕ добавляй ничего** — нажми "Добавить" только если нужны переменные окружения.

Для нашего проекта переменные **не нужны** (API отключен).

---

### 5. Команда запуска (если есть отдельное поле)
```
npm start
```

---

### 6. Порт (если есть отдельное поле)
```
3000
```

---

## Итоговые настройки

```
Команда сборки: npm install && npm run build
Зависимости: npm install (или пусто)
Директория сборки: dist
Переменные: не добавлять
```

---

## После заполнения

1. Проверь все поля
2. Убедись, что "Директория сборки" = `dist` (не `/build`!)
3. Нажми **"Заказать"** справа
4. Дождись завершения деплоя (2-5 минут)

---

## Если что-то пошло не так

### Ошибка: "Директория не найдена"
**Причина:** Указана неправильная директория сборки
**Решение:** Убедись, что указано `dist`, а не `/build` или `build`

### Ошибка: "Команда сборки не выполнена"
**Причина:** Неправильная команда или отсутствуют зависимости
**Решение:** 
- Проверь, что команда: `npm install && npm run build`
- Убедись, что в проекте есть `package.json`

### Ошибка: "Порт занят"
**Причина:** Порт 3000 уже используется
**Решение:** Timeweb обычно сам назначает порт, но можно указать другой (например, 8080)

---

**Готово!** После этих настроек сайт задеплоится. 🚀

