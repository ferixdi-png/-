# 🎯 Пошаговая инструкция для Timeweb

## Что выбрать в интерфейсе Timeweb

### Шаг 1: Тип приложения
✅ **Выбери:** `Frontend` (вкладка уже должна быть активна)

### Шаг 2: Фреймворк
✅ **Выбери:** `React` (синяя иконка с атомом)

**Почему React:**
- Наш проект использует React + Vite
- После сборки получается SPA (Single Page Application)
- Timeweb автоматически настроит команды сборки

**Альтернатива:**
- Если React недоступен → выбери `Другой`
- Тогда нужно будет вручную указать команды (см. ниже)

### Шаг 3: Источник кода

**Вариант A: GitHub (рекомендуется)**
1. Нажми на иконку GitHub на карточке React
2. Подключи свой GitHub аккаунт
3. Выбери репозиторий с проектом
4. Укажи ветку: `main` или `master`

**Вариант B: ZIP архив**
1. Собери проект: `npm run build`
2. Создай ZIP всего проекта (не только `dist/`)
3. Загрузи ZIP в Timeweb

### Шаг 4: Настройки приложения (5. Настройка приложения)

В разделе "5. Настройка приложения" укажи:

**1. Команда сборки:**
```
npm install && npm run build
```
(Или оставь `npm run build`, если зависимости устанавливаются автоматически)

**2. Зависимости:**
Оставь **пустым** или укажи:
```
npm install
```
(Обычно Timeweb сам устанавливает зависимости, но можно указать явно)

**3. Директория сборки:**
```
dist
```
⚠️ **Важно:** У нас не `/build`, а `dist` (Vite собирает в папку `dist`)

**4. Переменные:**
Нажми **"Добавить"** только если нужны переменные окружения.
Для нашего проекта **не нужны** (API отключен).

**5. Команда запуска (если есть поле):**
```
npm start
```

**6. Порт (если есть поле):**
```
3000
```

### Шаг 5: Регион и тариф

✅ **Регион:** Москва (или ближайший)
✅ **Тариф:** Базовый (99₽/мес)
- 200 тыс запросов/мес
- 1 ГБ NVMe
- Автодеплой включен

Этого достаточно для старта. Потом можно увеличить при необходимости.

### Шаг 6: Заказ

1. Нажми **"Заказать"** (фиолетовая кнопка справа)
2. Подтверди создание приложения
3. Дождись завершения сборки (2-5 минут)

### После деплоя

1. Получишь URL типа: `https://your-app-abc123.timeweb.app`
2. Открой его в браузере
3. Проверь в DevTools (F12 → Network):
   - ✅ Нет запросов к `*.googleapis.com`
   - ✅ Есть запросы к `fonts.bunny.net` и `esm.sh`

---

## Если что-то пошло не так

### Проблема: "Не удалось собрать проект"
**Решение:**
- Проверь, что в репозитории есть `package.json`
- Убедись, что команда `npm run build` работает локально
- Проверь логи в панели Timeweb

### Проблема: "Сайт не открывается"
**Решение:**
- Проверь, что порт указан правильно (3000)
- Убедись, что команда `npm start` работает
- Проверь логи деплоя

### Проблема: "Не могу выбрать React"
**Решение:**
- Выбери `Другой`
- Вручную укажи команды сборки (см. Шаг 4)

---

## Быстрая проверка перед деплоем

```bash
# 1. Убедись, что проект собирается
npm run build

# 2. Проверь локально
npm start
# Открой http://localhost:3000

# 3. Проверь, что нет Google-зависимостей
grep -r "googleapis\|gstatic" dist/ || echo "✅ Все чисто!"
```

---

**Готово!** После этих шагов твой сайт будет работать на Timeweb. 🚀

