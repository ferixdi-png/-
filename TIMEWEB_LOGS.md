# 🔍 Как найти логи деплоя в Timeweb

## Где найти логи

### Вариант 1: В панели приложения
1. Перейди в **"App Platform"** → выбери свое приложение
2. Открой вкладку **"Логи"** или **"Deploy Logs"**
3. Скопируй все логи и отправь в поддержку

### Вариант 2: В истории деплоев
1. В панели приложения найди раздел **"История деплоев"** или **"Deployments"**
2. Выбери последний деплой (обычно помечен как "Failed" или "Error")
3. Нажми на него — откроются логи
4. Скопируй все логи

### Вариант 3: Через TimewebGPT
1. Нажми кнопку **"TimewebGPT"** (в правом нижнем углу)
2. Спроси: "Покажи логи последнего деплоя"
3. Скопируй логи

---

## Возможные проблемы и решения

### Проблема 1: "Директория сборки не найдена"
**Ошибка в логах:**
```
Error: Build directory not found: /build
```

**Решение:**
- В настройках измени "Директория сборки" с `/build` на `dist`
- Пересобери проект

---

### Проблема 2: "Команда сборки не выполнена"
**Ошибка в логах:**
```
Error: npm run build failed
```

**Решение:**
- Измени "Команда сборки" на: `npm install && npm run build`
- Убедись, что в проекте есть `package.json`

---

### Проблема 3: "Модуль не найден"
**Ошибка в логах:**
```
Error: Cannot find module 'react'
```

**Решение:**
- В поле "Зависимости" укажи: `npm install`
- Или измени команду сборки на: `npm install && npm run build`

---

### Проблема 4: "Порт не указан"
**Ошибка в логах:**
```
Error: Port not specified
```

**Решение:**
- В настройках укажи порт: `3000`
- Или оставь пустым (Timeweb сам назначит)

---

## Что проверить перед отправкой логов

### 1. Локальная сборка работает?
```bash
npm install
npm run build
```
Если не работает — исправь ошибки локально, потом деплой.

### 2. Правильная директория сборки?
- У нас Vite → собирает в `dist`
- НЕ `/build`, НЕ `build` → именно `dist`

### 3. Правильная команда сборки?
```
npm install && npm run build
```

### 4. Есть ли `package.json`?
- Должен быть в корне проекта
- Должен содержать скрипт `build`

---

## Шаблон сообщения для поддержки

```
Здравствуйте!

У меня проблема с деплоем Frontend приложения на React.

Настройки:
- Фреймворк: React
- Команда сборки: npm install && npm run build
- Директория сборки: dist
- Node.js: 24

Логи деплоя:
[ВСТАВЬ СЮДА ЛОГИ]

Локально проект собирается успешно:
npm run build - работает
npm start - работает

Что может быть не так?
```

---

## Быстрая диагностика

### Проверь локально:
```bash
# 1. Установи зависимости
npm install

# 2. Собери проект
npm run build

# 3. Проверь, что папка dist создалась
ls dist/  # или dir dist на Windows

# 4. Запусти локально
npm start
```

Если все работает локально, но не работает на Timeweb — проблема в настройках деплоя.

---

## Альтернативное решение

Если деплой через App Platform не работает, можно использовать статический хостинг:

1. Собери проект локально: `npm run build`
2. Загрузи папку `dist/` на Timeweb Hosting через FTP
3. Настрой домен

Но лучше сначала попробовать исправить настройки деплоя.

---

**Отправь логи в поддержку, и они помогут решить проблему!** 🚀

