# 🔧 Исправление ошибки "Команда запуска не указана"

## Проблема
TimewebGPT говорит: **"Команда запуска не указана. Это критический параметр для запуска React приложения"**

## Решение

### Шаг 1: Открой настройки приложения
1. В панели Timeweb найди свое приложение
2. Перейди на вкладку **"Настройки"** (Settings)
3. Или открой настройки деплоя

### Шаг 2: Добавь команду запуска

Найди поле **"Команда запуска"** или **"Start Command"** или **"Запуск"**

Укажи:
```
npm start
```

**Или полная команда:**
```
serve -s dist -l 3000
```

### Шаг 3: Проверь все настройки

Убедись, что указано:

```
Команда сборки: npm install && npm run build
Зависимости: npm install (или пусто)
Директория сборки: dist
Команда запуска: npm start  ← ЭТО ГЛАВНОЕ!
Порт: 3000 (или оставь пустым)
```

### Шаг 4: Сохрани и пересобери

1. Сохрани настройки
2. Запусти новый деплой
3. Дождись завершения

---

## Альтернативное решение

Если поле "Команда запуска" не видно, попробуй:

### Вариант 1: Через package.json
Убедись, что в `package.json` есть скрипт:
```json
"scripts": {
  "start": "serve -s dist -l 3000"
}
```
(У тебя уже есть!)

### Вариант 2: Укажи в переменных окружения
Если есть раздел "Переменные окружения", добавь:
```
PORT=3000
NODE_ENV=production
```

### Вариант 3: Используй другой способ деплоя
Если App Platform не работает, можно:
1. Собери проект локально: `npm run build`
2. Загрузи папку `dist/` на Timeweb Hosting через FTP
3. Настрой статический хостинг

---

## Проверка после исправления

После добавления команды запуска:

1. **Сохрани настройки**
2. **Запусти новый деплой**
3. **Проверь логи** — должны быть сообщения:
   - ✅ "Build successful"
   - ✅ "Starting application"
   - ✅ "Server running on port 3000"

---

## Если все еще не работает

### Проверь локально:
```bash
npm install
npm run build
npm start
```

Если локально работает, а на Timeweb нет — проблема в настройках Timeweb.

### Что проверить:
- ✅ Команда запуска указана: `npm start`
- ✅ Директория сборки: `dist` (не `/build`)
- ✅ Команда сборки: `npm install && npm run build`
- ✅ В `package.json` есть скрипт `start`
- ✅ Зависимость `serve` установлена (есть в `package.json`)

---

**Главное:** Добавь команду запуска `npm start` в настройках приложения! 🚀

