# ✅ Решение проблемы "Команда запуска не указана"

## Проблема
Timeweb требует явно указать команду запуска отдельно от команды сборки, но в интерфейсе нет такого поля.

## Решение: Используй переменные окружения

### Шаг 1: В настройках Timeweb

**Команда сборки:**
```
npm install && npm run build
```

**Зависимости:**
Оставь пустым

**Директория сборки:**
```
dist
```

**Переменные (ВАЖНО!):**
Нажми **"Добавить"** и добавь переменную:

- **Имя переменной:** `START_COMMAND`
- **Значение:** `npm start`

Или попробуй:
- **Имя:** `NPM_START`
- **Значение:** `node server.js`

---

## Альтернативное решение: Статический хостинг

Если App Platform не работает из-за отсутствия поля "Команда запуска", используй **статический хостинг** - это проще для SPA:

### Шаг 1: Собери проект локально
```bash
npm install
npm run build
```

### Шаг 2: Загрузи на хостинг
1. В панели Timeweb перейди в **"Хостинг"** (не App Platform)
2. Создай новый сайт
3. Загрузи все файлы из папки `dist/` в корень сайта
   - Через FTP
   - Или через файловый менеджер

### Шаг 3: Готово!
Сайт будет работать без команды запуска.

**Преимущества:**
- ✅ Не нужна команда запуска
- ✅ Проще и дешевле
- ✅ Работает сразу
- ✅ Идеально для статических сайтов (SPA)

---

## Если все-таки нужен App Platform

### Вариант 1: Через переменные (попробуй разные имена)

Добавь переменные окружения с разными именами:

1. `START_COMMAND` = `npm start`
2. `NPM_START` = `npm start`
3. `START_SCRIPT` = `node server.js`
4. `CMD` = `npm start`

Timeweb может искать команду запуска по одному из этих имен.

### Вариант 2: Создай файл конфигурации

Я создал файл `.timeweb.json` - попробуй загрузить проект с этим файлом, возможно Timeweb его прочитает.

### Вариант 3: Используй другой формат деплоя

В настройках деплоя попробуй выбрать:
- "Docker" вместо "Frontend"
- Или "Backend" с явным указанием команды

---

## Рекомендация

**Для React SPA приложения лучше использовать статический хостинг:**

1. Собери проект: `npm run build`
2. Загрузи папку `dist/` на Timeweb Hosting
3. Настрой домен
4. Готово!

Это проще, дешевле и надежнее для статических сайтов.

---

## Проверка package.json

Убедись, что в `package.json` есть:

```json
{
  "scripts": {
    "build": "vite build",
    "start": "node server.js"
  }
}
```

✅ У тебя уже все правильно настроено!

---

## Итог

**Попробуй в таком порядке:**

1. **Сначала:** Добавь переменную `START_COMMAND` = `npm start` в настройках
2. **Если не работает:** Используй статический хостинг (проще всего)
3. **Если нужен App Platform:** Попробуй разные имена переменных

**Самый простой вариант - статический хостинг!** 🚀

